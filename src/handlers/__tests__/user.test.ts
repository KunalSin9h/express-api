describe("Trying Jest", () => {
    test("Should Pass", () => {
        expect(1).toBe(1);
    })
    test("Should Pass", () => {
        expect(1).toBe(1);
    })
});

describe("Trying Jest 2", () => {
    test("Should Pass", () => {
        expect(1).toBe(1);
    })
    test("Should Pass", () => {
        expect(1).toBe(1);
    })
});